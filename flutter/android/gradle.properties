org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Optional: define the same MYAPP_UPLOAD_* release signing properties used by the
# React Native app here, or in ~/.gradle/gradle.properties. This project also
# falls back to react_native/android/gradle.properties when present.
