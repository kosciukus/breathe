<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Declare this is a Wear OS app -->
    <uses-feature android:name="android.hardware.type.watch" />

    <!-- Vibration for haptic feedback on phase transitions -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Keep screen on during a session -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Health Connect: log mindfulness sessions -->
    <uses-permission android:name="android.permission.health.WRITE_MINDFULNESS_SESSION" />
    <uses-permission android:name="android.permission.health.READ_MINDFULNESS_SESSION" />

    <application
        android:label="Breathe"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="breathe" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Standalone Wear OS app — no phone app required -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>
</manifest>
